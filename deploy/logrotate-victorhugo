# /etc/logrotate.d/victorhugo
# Rotate the daily backup log. systemd-journald handles app logs separately.

/var/log/victorhugo-backup.log {
    weekly
    rotate 8
    compress
    delaycompress
    missingok
    notifempty
    create 644 victorhugo victorhugo
}
