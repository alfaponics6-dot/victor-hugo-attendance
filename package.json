{
  "name": "automation-studio",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Browser-based automation rules engine — define WHEN/THEN rules, fire events, watch them run.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "vite": "^6.0.7"
  }
}
